# Kufar Support Bot

Модули:

| Файл / папка | Назначение |
|--------------|------------|
| `main.py` | Запуск бота и polling Telegram |
| `poller.py` | Фоновая рассылка объявлений |
| `user_matching.py` | Сопоставление объявлений с фильтрами пользователя |
| `handlers/` | Telegram: `start`, `nav`, `goods`, `admin` — см. [ARCHITECTURE.md](ARCHITECTURE.md) |
| `kufar_fetch.py` | API Kufar |
| `filters.py` | Отбор объявлений |
| `bot_ui.py` | Тексты и клавиатуры |
| `db.py`, `config.py`, `formatter.py` | База, настройки, формат сообщений |

Цены в интерфейсе — **белорусские рубли** (обозначение **Br**, код BYN), как на Kufar.by.

## Запуск

Скопируйте [`.env.example`](.env.example) в `.env`, укажите `TOKEN` и `ADMIN_IDS`. Установите зависимости: `pip install -r requirements.txt`, затем `python main.py`.

**Логи (продакшен):** по умолчанию тихий режим — одна строка при старте, без спама каждый цикл опроса. В `.env`:

- `LOG_LEVEL=INFO` — обычный (по умолчанию)
- `LOG_LEVEL=DEBUG` — детали опроса Kufar, кнопки, полные traceback при ошибках
- `FILTER_DEBUG_LOG=true` — почему отбрасываются объявления (только при отладке фильтров)

Тесты (необязательны на сервере): `python -m unittest discover -v` — локальная проверка фильтров и рефералки после правок кода.

## База данных и Git

Файлы SQLite **не коммитятся** (см. `.gitignore`: `bot.db`, `data/`).

### Локально

По умолчанию база: **`data/bot.db`** в папке проекта. При первом запуске после обновления старый `bot.db` из корня проекта копируется в `data/` автоматически.

### BotHost.ru (деплой из GitHub)

При каждом push пересобирается контейнер; **`bot.db` в корне репозитория стирается**. На BotHost есть постоянная папка **`/app/data`** — она **не** перезаписывается из Git.

Бот сам кладёт базу в `/app/data/bot.db`, если эта папка есть (типично на BotHost). Дополнительно в панели бота можно задать:

```env
DB_PATH=/app/data/bot.db
```

**Один раз после обновления:** если важные данные остались в старом файле на сервере, через файловый менеджер BotHost скопируйте `bot.db` в папку **`data`** бота (путь `/app/data/bot.db`), затем перезапустите.

- Делайте **резервные копии** `/app/data` или `data/bot.db` перед крупными обновлениями.
- В продакшене — **один** процесс бота и **одна** БД.

## Средняя цена и инстансы

Средняя рыночная цена для VIP считается по таблице `market_prices` в **одной** базе за последние **14 дней** (настраивается: `PRICE_DATA_RETENTION_DAYS` в `.env`). Записи старше этого срока автоматически удаляются из `market_prices` и `sent_prices`. Чтобы у всех пользователей было одно и то же число для одной модели:

- в продакшене должен работать **один** процесс бота и **одна** БД по `DB_PATH`;
- несколько копий бота с разными файлами SQLite дадут разные средние.

После каждого запроса к Kufar бот дополняет `market_prices` только из **целых телефонов** (без коробок, стёкол, плат и т.п.).

**Фильтры рассылки:** у **обычных** — цена, модели, память и отсев коробок/аксессуаров; **полный жёсткий** отбор («куплю», «новый» и т.д.) — только у **VIP**. В `market_prices` попадают только цены из рассылки **VIP** (не из обычной).

### VIP-потоки

Во вкладке **VIP** (только для VIP) можно переключить поток рассылки:

| Поток | Описание |
|-------|----------|
| Обычная | Все подходящие лоты по моделям, памяти и цене |
| Ниже рынка | Только дешевле средней по `market_prices` |
| Обмен | Только объявления с обменом |
| **Идеальные (бета)** | «Б/у» на Kufar допускается; плохие состояния отсекаются; после загрузки описания — явный % АКБ ≥ `IDEAL_MIN_BATTERY_PERCENT` (75), без поломок/замен; царапины не учитываются |

**Охват листинга:** `KUFAR_SIZE` объявлений на **страницу** (по умолчанию 40), `KUFAR_MAX_PAGES` страниц на запрос (по умолчанию 2, cursor API). Итого до ~80 объявлений на каждый запрос из `KUFAR_QUERY`.

**Просмотренные объявления:** записи в `seen_ads` старше `SEEN_ADS_RETENTION_DAYS` (по умолчанию 90) удаляются автоматически.

**Интервалы рассылки VIP/обычных** сохраняются в БД (`poll_last_vip`, `poll_last_regular`) — после перезапуска бота нет «залпа» всем подписчикам сразу.

## Фильтр памяти

В меню **«Память»** выбираются объёмы: 64, 128, 256, 512 GB и **более** (от 512 GB, включая 1 ТБ).

- **Обычный аккаунт:** одна модель и один объём памяти; новый выбор заменяет прежний. По умолчанию при регистрации — **64 GB**.
- **VIP:** можно выбрать несколько моделей и объёмов; снять последний объём нельзя (останется минимум 64 GB). При **окончании или снятии** VIP модели обрезаются до одной (первая в списке), память — на **64 GB**; лимит цены при естественном окончании сохраняется (при снятии админом — сброс цены на default).
- Если в объявлении Kufar память **не указана**, оно проходит под любой выбранный объём.

В БД: поле `users.memory_volumes` (CSV, например `64,128` или `512+`).

## Реферальная программа

Во вкладке **VIP** — персональная ссылка вида `https://t.me/<бот>?start=ref_<код>`.

- Пригласивший получает **+1 день VIP** за каждого **нового** пользователя, который впервые открыл бота по этой ссылке (`REFERRAL_VIP_DAYS_PER_FRIEND` в `.env`, по умолчанию 1).
- Повторный `/start` того же друга или реактивация старого аккаунта бонус **не** дают.
- Свою ссылку использовать нельзя.

Таблица `referrals` фиксирует, за кого бонус уже начислен.

## Оплата VIP (RollyPay)

Онлайн-оплата тарифов VIP (7 / 30 / 90 дней) через [RollyPay](https://docs.rollypay.io). После 100% оплаты VIP выдаётся автоматически (webhook + резервный опрос статуса).

**BotHost:**

1. Включите домен у бота («Использовать домен»), запомните URL / переменную `DOMAIN`.
2. Порт в панели = `PORT` в `.env` (код слушает `0.0.0.0:$PORT`).
3. В `.env` панели:

```env
ROLLYPAY_ENABLED=true
ROLLYPAY_API_KEY=...
ROLLYPAY_SIGNING_SECRET=...
ROLLYPAY_TERMINAL_ID=dd737481-bbfd-46a9-992c-feb99069bb23
PUBLIC_BASE_URL=https://ваш-домен.bothost.ru
PORT=8080
```

4. В панели RollyPay → касса → **Callback URL** = `{PUBLIC_BASE_URL}/webhooks/rollypay`.
5. После деплоя откройте `https://…/health` — должен ответить `{"ok":true,…}`.

Секреты только в панели BotHost, не в git.

## Правовая информация

Документы для пользователей:

- [Политика конфиденциальности](https://telegra.ph/POLITIKA-KONFIDENCIALNOSTI-08-12-99)
- [Пользовательское соглашение (публичная оферта)](https://telegra.ph/PUBLICHNAYA-OFERTA-08-12-15)
- Поддержка: [@KufiBY](https://t.me/kufiBY)

Ссылки также в боте: **Помощь** и вкладка **VIP**.

## Тестирование при живых пользователях

- Отдельный **стейджинг**: второй токен бота (`TOKEN`) и отдельный **`DB_PATH`**, чтобы не трогать продакшен-данные.
- Изменения в **ветке**, проверка на стейдже, затем merge в основную ветку и деплой.
